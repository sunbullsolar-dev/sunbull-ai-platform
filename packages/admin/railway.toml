[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm install -g serve && npx vite build"
watchPatterns = ["packages/admin/**"]

[deploy]
startCommand = "serve dist -s -l 3001"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
